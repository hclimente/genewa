profiles {

    standard {
        process.executor = 'local'
    }

    cbio {
        process.executor = 'sge'
        process.queue = 'all.q'
    }

    curie {
        process.executor = 'pbs'
        process.queue = 'batch'

        executor {
            queueSize = 200
        }
    }
}
